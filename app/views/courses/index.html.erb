<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Index page</title>
</head>
<body>
  <a href="/">Home</a>
<br>
  <%= link_to "Create new course", new_course_path %>

  <h2 id="intro" style="padding: 1rem 1.5rem 1rem 1.5rem;">Index page - List of courses saved in the database</h2>

  <article id="intro" style="padding: 3rem 1.5rem 1rem 1.5rem;">
    <td> <% @courses.each do |course| %> 
      <div class="card">
        <div class="card-body">
        <h3 class="card-text"> <%= course.name %> </h3> 
          <p class="card-text"><%= course.description %> 
            <br> <%= course.date %>  
            <br> <%= course.time %> 
            <br> <%= link_to "Delete", course, method: :delete, data: {confirm:  "You Sure?"} %>
          </p>
        </div>
      </td>
    <% end %>
  </article>
</body>
</html>